# ── Database ───────────────────────────────────────────────────────────────────
# Use plain IP/hostname, no protocol prefix
DB_HOST=your-db-host-or-ip
DB_USER=your-db-user
DB_PASS=your-db-password
DB_NAME=your-db-name

# ── Server ─────────────────────────────────────────────────────────────────────
PORT=3001
# Must match the frontend domain exactly (no trailing slash)
CLIENT_URL=http://localhost:3001
# Used to convert relative og:image paths to absolute URLs for social crawlers
SITE_URL=http://localhost:3001

# ── Admin Auth ─────────────────────────────────────────────────────────────────
# Generate JWT_SECRET with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_USER=your-admin-username
ADMIN_PASS=your-strong-admin-password
JWT_SECRET=your-64-char-hex-secret

# ── Booking / Razorpay (managed via Admin → Booking → Payment Gateway) ─────────
# These values are stored in the DB and managed through the admin panel.
# No need to set them here — configure via /admin/booking after running migrations.

# ── Booking / Email SMTP (managed via Admin → Booking → Email Notifications) ──
# These values are stored in the DB and managed through the admin panel.
# Configure via /admin/booking → Email Notifications tab after running migrations.
